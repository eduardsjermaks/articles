# Role
You are acting as an impartial Senior Software Architect and Evaluation Model. You are evaluating two architecture orientation documents for the same repository.

# Context & Constraints
- You do NOT have access to the source code repository. 
- You must evaluate strictly based on the provided text.
- **Bias Warning:** Do NOT prefer verbosity, professional formatting, or "corporate" writing styles. Penalize flowery language and generic definitions.
- **Preference:** Reward technical correctness, internal consistency, cited evidence within the text (e.g., file paths, library names), and rigorous reasoning discipline.

# Pro-Level Directives
- **Chain of Thought First:** Before outputting any scores, provide a detailed step-by-step analysis. Identify internal contradictions, compare specific technical claims, and look for "hallucination markers" (generic claims) versus "grounded markers" (specific file paths/logic flows).
- **Gap Analysis:** Look for the "negative space." What critical architectural domains (e.g., authentication, error handling, CI/CD, data persistence) are completely omitted or glossed over?
- **Implicit vs. Explicit:** Heavily differentiate between what a document implies and what it explicitly proves.
- **Penalty Justification:** If you award a score of 3 or below in any category, you MUST provide a direct quote of the vague, contradictory, or fluffy text that earned the penalty.

# Evaluation Rubric (Score 0–5 per category)
1. **Evidence Grounding:** Does the document cite specific modules, files, or patterns rather than vague generalities?
2. **Structural Accuracy:** Is the internal logic consistent? Do the described components actually fit together logically?
3. **Dependency Mapping:** How well does it identify external integrations, internal coupling, and third-party libraries?
4. **Critical Flow Identification:** Does it map the "Happy Path" of data through the system (Entry point -> Logic -> Storage)?
5. **Migration Insight Quality:** Does it identify technical debt, "gotchas," legacy patterns, or risks that would impact a migration?
6. **Epistemic Discipline:** How does it handle uncertainty? Does it clearly distinguish between known facts ("The system does X") and assumptions ("The system appears to do X")?
7. **Signal-to-Noise Ratio:** Is the document concise and information-dense, or is it filled with filler?

# Required Output Structure

## 1. Initial Analysis & Gap Detection (Your "Thinking" Phase)
- Outline your analysis of both documents.
- Note any contradictions, lack of evidence, or missing critical architectural components.

## 2. Document A Evaluation
- **Scores:** [List 1–7 with 0-5 scores]
- **Justification:** Explain each score. Quote the text to prove why it succeeded, or quote the exact text that caused a penalty (for scores <= 3).

## 3. Document B Evaluation
- **Scores:** [List 1–7 with 0-5 scores]
- **Justification:** Explain each score. Quote the text to prove why it succeeded, or quote the exact text that caused a penalty (for scores <= 3).

## 4. Head-to-Head Comparison
- **Conflict Resolution:** If Doc A and Doc B contradict each other on a technical point, analyze which one provides better logical reasoning or supporting evidence.
- **Depth & Reliability:** Compare the signal-to-noise ratio and epistemic discipline of both.

## 5. Final Verdict
- **The Safer Document for Migration:** Which document would a Lead Engineer trust more for a high-stakes migration?
- **Core Reasoning differences:** The primary technical differentiator between the two.
- **Confidence Level:** (Low / Medium / High) - Explain why.

---

# Project Orientation (Generated)

> Generated by a first-pass code review of the `develop` branch (commit `08801ed`).
> Epistemic labels: **FACT** = directly observable in code · **INFERENCE** = deduced from structure · **UNKNOWN** = no evidence found. Claims with fewer than 2 independent code references are marked **Tentative**.

---

## 1. What this project is

**Purpose**
- **FACT**: nopCommerce is an open-source eCommerce platform ("shopping cart") built on ASP.NET Core (README.md:1-16). The solution is a single ASP.NET Core MVC web application plus supporting class libraries and plugins (src/NopCommerce.sln, src/Presentation/Nop.Web/Program.cs).
- **FACT**: It is cross-database — SQL Server, MySQL, and PostgreSQL providers exist side by side (src/Libraries/Nop.Data/DataProviders/MsSqlDataProvider.cs, MySqlDataProvider.cs, PostgreSqlDataProvider.cs).

**Who uses it / main use-cases**
- **INFERENCE**: Two user populations are visible in code: public shoppers (storefront controllers such as `CatalogController`, `ShoppingCartController`, `CheckoutController` in src/Presentation/Nop.Web/Controllers/) and store administrators (a full admin area at src/Presentation/Nop.Web/Areas/Admin/ with its own Controllers, Factories, Models, Views).
- **FACT**: Multi-store, multi-vendor, and multi-language are first-class: `IStoreContext` (src/Libraries/Nop.Core/IStoreContext.cs), `Nop.Core/Domain/Vendors/`, `Nop.Core/Domain/Localization/`, `Nop.Services/Localization/`.

---

## 2. Quick start (developer)

**Prereqs**
- **FACT**: .NET SDK **10.0.100** (rollForward latestFeature) is pinned (global.json:1-7). The Dockerfile builds against `dotnet/sdk:10.0-alpine` (Dockerfile:2).
- **FACT**: Node.js is used for frontend asset tooling — `gulpfile.js`, `package.json` exist in src/Presentation/Nop.Web/ and the Azure Kudu deploy script checks for node (src/deploy.cmd:12-17). **Tentative**: whether gulp is required for a plain local run is not verified; the app may serve pre-built assets from wwwroot.
- **FACT**: A database: SQL Server (docker-compose.yml uses `mcr.microsoft.com/mssql/server:2019-latest`), or MySQL / PostgreSQL (mysql-docker-compose.yml, postgresql-docker-compose.yml).

**How to run locally**
- **FACT**: The single runnable entrypoint is `Nop.Web` — `dotnet Nop.Web.dll` is the container ENTRYPOINT (Dockerfile:57) and `Program.Main` builds/runs the WebApplication (src/Presentation/Nop.Web/Program.cs:11-50).
- **FACT**: Docker path: `docker-compose up` builds the app image from the root Dockerfile and starts an MSSQL Express container on port 80 (docker-compose.yml).
- **FACT**: First run is guided by a web installer: `InstallController` drives `IInstallationService` (src/Presentation/Nop.Web/Controllers/InstallController.cs:23-57; src/Libraries/Nop.Services/Installation/). Database connection settings are then persisted (see §7).

**How to run tests**
- **FACT**: One test project, `Nop.Tests`, using NUnit 4 + Moq + AwesomeAssertions, with an in-memory/SQLite-backed data provider for tests (src/Tests/Nop.Tests/Nop.Tests.csproj:15-24; src/Tests/Nop.Tests/SqLiteNopDataProvider.cs). Run with `dotnet test src` — this is exactly what CI does (.github/workflows/dotnet.yml, "Test" step).

**Common pitfalls**
- **FACT**: Config is *not* in the conventional root `appsettings.json`; it is loaded from `App_Data/appsettings.json` (src/Libraries/Nop.Core/Configuration/NopConfigurationDefaults.cs:11-17; wired in Program.cs:14-19). If you look in the project root you will find nothing.
- **FACT**: Plugins compile into `Presentation/Nop.Web/Plugins/<PluginName>` via `<OutputPath>` overrides (e.g. src/Plugins/Nop.Plugin.Payments.Manual/Nop.Plugin.Payments.Manual.csproj:12-13), and a custom build step `ClearPluginAssemblies` exists (src/Build/ClearPluginAssemblies.proj). **INFERENCE**: build the whole solution, not just Nop.Web, or plugins will be missing/stale.
- **FACT**: Docker runtime requires writable dirs (`App_Data`, `logs`, `Plugins`, several `wwwroot` subdirs — Dockerfile:20-33); permission problems on these are a plausible failure mode when self-hosting. (**Tentative** as a pitfall — inferred from the chmod list, not from documented incidents.)

---

## 3. Architecture at a glance

**High-level component diagram** (FACT for boxes/arrows — from `ProjectReference` entries):

```
                    ┌────────────────────────────┐
                    │  Nop.Web (ASP.NET Core MVC)│  ← single runtime process
                    │  - Public storefront       │
                    │  - /Admin area             │
                    │  - Web installer           │
                    │  - Plugins/ (drop-in DLLs) │
                    └──────────────┬─────────────┘
                                   │ refs (Nop.Web.csproj:22-25)
                    ┌──────────────▼─────────────┐
                    │  Nop.Web.Framework          │  MVC plumbing: filters, DI
                    └──────────────┬─────────────┘  wiring, themes, factories
                                   │ refs (Nop.Web.Framework.csproj:26-28)
                    ┌──────────────▼─────────────┐
                    │  Nop.Services               │  ~40 domain service folders
                    └──────┬──────────────┬──────┘  (Catalog, Orders, Payments…)
                           │              │ refs (Nop.Services.csproj:32-33)
              ┌────────────▼───┐   ┌──────▼───────┐
              │  Nop.Data      │──▶│  Nop.Core    │  entities, config, engine,
              │  linq2db +     │   │  (no project │  caching, events
              │  FluentMigrator│   │   deps)      │
              └───────┬────────┘   └──────────────┘
                      │
            ┌─────────▼──────────┐
            │ SQL Server │ MySQL │ PostgreSQL (DataProviders/)
            └────────────────────┘
```

**Runtime processes**
- **FACT**: One process. There is no separate worker/CLI project in the solution (src/NopCommerce.sln contains only Nop.Core, Nop.Data, Nop.Services, Nop.Web.Framework, Nop.Web, Nop.Tests, plugins).
- **FACT**: Background jobs run *inside* the web process via a `TaskScheduler` that fires HTTP POSTs to the app's own endpoint `scheduletask/runtask` (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:67,195-206; NopTaskDefaults.cs:11; handled by src/Presentation/Nop.Web/Controllers/ScheduleTaskController.cs:23).
- **INFERENCE**: A `KeepAliveController` (src/Presentation/Nop.Web/Controllers/KeepAliveController.cs) exists to keep the site warm so those scheduled tasks keep firing.

**Data stores and queues**
- **FACT**: Relational DB is the only persistent store; access goes through `IRepository<T>`/`EntityRepository` on linq2db (src/Libraries/Nop.Data/IRepository.cs, EntityRepository.cs; linq2db in Nop.Data.csproj:19).
- **FACT**: No message broker. Events are in-process via `IEventPublisher` (src/Libraries/Nop.Core/Events/IEventPublisher.cs, EventPublisherExtensions.cs).
- **FACT**: Caching supports in-memory, Redis, RedisSynchronizedMemory (default enum value), and SqlServer distributed cache (src/Libraries/Nop.Core/Configuration/DistributedCacheConfig.cs:15-45; StackExchangeRedis + Caching.SqlServer packages in Nop.Core.csproj:21-22).

---

## 4. Repository map

| Path | Responsibility (FACT unless noted) |
|---|---|
| `src/Libraries/Nop.Core` | Base entity (`BaseEntity.cs`), domain POCOs for ~30 areas (`Domain/`), app configuration (`Configuration/AppSettings.cs`), engine/DI bootstrap (`Infrastructure/NopEngine.cs`), caching, in-process events (`Events/`), `IWorkContext`/`IStoreContext` ambient context abstractions. |
| `src/Libraries/Nop.Data` | Persistence: `IRepository<T>`, per-DB providers (`DataProviders/`), FluentMigrator-based schema migrations incl. versioned upgrade folders `UpgradeTo440/450/460` (`Migrations/`), connection settings management (`DataSettingsManager.cs`). |
| `src/Libraries/Nop.Services` | Business logic, one folder per subdomain: Catalog, Orders, Payments, Shipping, Tax, Customers, Discounts, Messages, Gdpr, Security, Plugins, ScheduleTasks, ExportImport, Installation, etc. Key public interfaces: `IOrderProcessingService`, `IShoppingCartService`, `IPaymentService`, `IPermissionService`, `IPluginService`. |
| `src/Presentation/Nop.Web.Framework` | Shared MVC infrastructure: DI registration (`Infrastructure/Extensions/ServiceCollectionExtensions.cs`), request pipeline (`ApplicationBuilderExtensions.cs`), filters, themes, model factories base. |
| `src/Presentation/Nop.Web` | The application: public controllers, `Areas/Admin`, Razor views + `Themes/`, `Factories/` (view-model assembly), gulp asset pipeline, `App_Data/` runtime config, `Plugins/` runtime plugin output. |
| `src/Plugins/*` | ~33 first-party plugins by category prefix: `Payments.*`, `Shipping.*`, `Tax.*`, `Widgets.*`, `Misc.*` (incl. `Misc.WebApi.Frontend` REST API and `Misc.NopMobileApp`), `ExternalAuth.Facebook`, `MultiFactorAuth.GoogleAuthenticator`, `Search.Lucene`. |
| `src/Tests/Nop.Tests` | Single test project mirroring layers: `Nop.Core.Tests`, `Nop.Data.Tests`, `Nop.Services.Tests`, `Nop.Web.Tests`. |
| `src/Build` | Custom MSBuild step `ClearPluginAssemblies` used during plugin builds. |
| `upgradescripts/` | Historical SQL upgrade scripts per version range (1.90-2.00 … ), predating/complementing FluentMigrator. |
| Root | `Dockerfile`, three docker-compose variants (mssql/mysql/postgres), `global.json` (SDK pin), `.github/workflows/dotnet.yml` (CI). |

---

## 5. Critical flows

### 5.1 Order placement (checkout → payment → persistence → side-effects)
- **Trigger/entrypoint**: `CheckoutController` (public storefront) collects addresses/shipping/payment and calls `IOrderProcessingService` (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:33-60 — the controller injects `IOrderProcessingService`, `IPaymentService`, `IShoppingCartService`).
- **Steps** (FACT — src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1594-1720):
  1. `PlaceOrderAsync` validates the request and builds a `PlaceOrderContainer` via `PreparePlaceOrderDetailsAsync` (:1594-1603).
  2. Payment is processed (`GetProcessPaymentResultAsync`, :1610-1612) — delegates to the configured payment plugin (**INFERENCE** from `IPaymentPluginManager` usage in CheckoutController.cs:51).
  3. On success: order saved (`SaveOrderDetailsAsync`), cart items moved to order items, discount & gift-card usage history saved, recurring payment created if applicable (:1616-1632).
  4. Side-effects: notifications sent, checkout data reset, activity logged, `OrderPlacedEvent` published, order status checked, `ProcessOrderPaidAsync` if already paid (:1633-1651).
- **Failure modes** (FACT): exceptions are caught, logged via `ILogger`, and surfaced as result errors — the order simply fails (:1661-1676). Duplicate-submission protection uses a **named OS `Mutex` keyed by customer id** plus a cache flag, with explicitly synchronous `.Result`/`.Wait()` calls because "mutexes cannot be used with the await operation" (:1680-1718). **INFERENCE**: this is single-machine protection only; in a web farm the mutex does not span nodes (the cache flag partially does, if the cache is distributed). This is the costliest place for errors: payment succeeded but order-save failed would need manual reconciliation — **Tentative**, compensation logic was not located.

### 5.2 Scheduled/background tasks
- **Trigger**: in-process `TaskScheduler` timers (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:263-267) POST to `{storeUrl}/scheduletask/runtask` (:67, 195-206).
- **Steps**: `ScheduleTaskController.RunTask(taskType)` resolves and runs the task via `IScheduleTaskRunner` (src/Presentation/Nop.Web/Controllers/ScheduleTaskController.cs:23-…). Example tasks: `ClearLogTask` (Logging/), `DeleteInactiveCustomersTask` (Gdpr/).
- **Failure modes** (FACT): HTTP errors are caught and logged with the task type and URL (TaskScheduler.cs:221). **INFERENCE**: if the site is idle/stopped, tasks don't run — hence KeepAlive.

### 5.3 Application startup / plugin loading
- **Trigger**: `Program.Main` → `ConfigureApplicationServices` → `NopEngine`, which discovers `INopStartup` and `IStartupTask` implementations by reflection across all assemblies including plugins (src/Presentation/Nop.Web/Program.cs:11-50; src/Libraries/Nop.Core/Infrastructure/NopEngine.cs:40-150).
- **Side effects**: DB migrations can run at startup via `MigrationManager`/`NopDbStartup` (src/Libraries/Nop.Data/Migrations/MigrationManager.cs, src/Libraries/Nop.Data/NopDbStartup.cs). **Tentative** on exact conditions (fresh install vs upgrade) — inspect `NopDbStartup.cs` and `Installation/` migrations.
- **Failure modes**: **INFERENCE** — a bad plugin DLL in `Plugins/` participates in reflection scanning and DI registration and can take down startup; the `ClearPluginAssemblies` build step (src/Build/) exists to manage plugin assembly conflicts.

---

## 6. Dependencies

**Internal module dependencies** (FACT, from csproj ProjectReferences — strictly acyclic, one direction):
`Nop.Web` → `Nop.Web.Framework` → `Nop.Services` → `Nop.Data` → `Nop.Core` (Nop.Web.csproj:22-25; Nop.Web.Framework.csproj:26-28; Nop.Services.csproj:32-33; Nop.Data.csproj:27).

**External services** (FACT — all optional/plugin-gated unless noted):
- Database (required): SQL Server / MySQL / PostgreSQL (Nop.Data.csproj:20-22).
- Redis or SQL Server distributed cache (optional) (Nop.Core.csproj:21-22; DistributedCacheConfig.cs).
- SMTP mail via MailKit (Nop.Services.csproj:20; src/Tests/.../TestSmtpBuilder.cs confirms an SMTP builder abstraction).
- MaxMind GeoIP2 with a bundled `GeoLite2-Country.mmdb` (Nop.Services.csproj:22; src/Presentation/Nop.Web/App_Data/GeoLite2-Country.mmdb).
- Azure: Azure.Identity in core (Nop.Core.csproj:18), Azure Blob storage plugin (src/Plugins/Nop.Plugin.Misc.AzureBlob).
- Third-party via plugins: PayPal Commerce, UPS, Twilio SMS, Avalara tax, Zettle, Brevo, Dynamics 365, ECB exchange rates, Facebook auth/pixel, Google Analytics, Cloudflare Images (src/Plugins/*).

**Build/deploy dependencies** (FACT):
- .NET SDK 10 (global.json), NUnit toolchain (Nop.Tests.csproj), gulp/node for assets (Nop.Web/gulpfile.js, package.json), Docker (Dockerfile + 3 compose files), GitHub Actions on windows-latest (.github/workflows/dotnet.yml), Azure Kudu script (src/deploy.cmd), custom `ClearPluginAssemblies` MSBuild step (src/Build/).

---

## 7. Configuration & environments

- **Where config lives** (FACT): `App_Data/appsettings.json` + `App_Data/appsettings.{Environment}.json`, then environment variables, in that order (Program.cs:14-20; NopConfigurationDefaults.cs:11-17). Strongly-typed `IConfig` sections are aggregated into a singleton `AppSettings` (src/Libraries/Nop.Core/Configuration/AppSettings.cs, AppSettingsHelper.cs).
- **FACT**: The app *writes* its own config: `AppSettingsHelper.SaveAppSettings` persists configs back to the JSON file (AppSettingsHelper.cs:28) — e.g. after the web installer saves DB settings (`DataSettingsManager.cs:111-119`, which also migrates legacy `Settings.txt` / `dataSettings.json` formats, NopDataSettingsDefaults.cs:11-16).
- **Environment variables** (FACT): supported globally via `AddEnvironmentVariables()` (Program.cs:20). Specific variable names beyond ASP.NET defaults (`ASPNETCORE_URLS` set in Dockerfile:55): **UNKNOWN** — inspect `AppSettingsHelper` and docs for the env-var naming convention for config sections.
- **Secrets handling** (FACT): DB connection string lives in `App_Data/appsettings.json` on disk; DataProtection keys persist to the file system at `App_Data/DataProtectionKeys` (ServiceCollectionExtensions.cs:242-248; Dockerfile chmod list). **INFERENCE**: no vault/KeyVault integration is wired in the core config path (Azure.Identity is referenced but its use was not located — **Tentative**). Treat `App_Data` as sensitive.
- **Local vs prod differences** (FACT): per-environment appsettings file (Program.cs:16-19); DI container validation differs (`ValidateScopes=false`, Program.cs:31-38); optional Autofac vs default DI switch via `CommonConfig.UseAutofac` (Program.cs:25-29). Kubernetes manifests: **UNKNOWN** (none found in repo; deployment beyond Docker/Kudu is out of tree).

---

## 8. Observability

- **Logging** (FACT): custom `ILogger` (`Nop.Services.Logging.ILogger`) writing to the **database** via `IRepository<Log>` (src/Libraries/Nop.Services/Logging/DefaultLogger.cs:20-133; entity src/Libraries/Nop.Core/Domain/Logging/Log.cs). A `ClearLogTask` prunes it. Activity auditing via `CustomerActivityService` (same folder). A `logs/` directory is created in the Docker image (Dockerfile:18) — **Tentative**: what writes to it was not located (possibly ASP.NET/stdout or plugin logs).
- **Metrics** (FACT): no Prometheus/OpenTelemetry/App Insights packages found in any csproj (grep across src). **UNKNOWN**: any metrics beyond what plugins (e.g. Google Analytics widget — client-side) provide.
- **Tracing** (FACT): no distributed tracing packages found. **UNKNOWN** otherwise.
- **Alerts**: **UNKNOWN** — nothing in repo. Inspect hosting-level tooling if you need alerting.
- **INFERENCE**: observability is DB-log-centric and admin-UI-oriented (admin area has Log/activity screens under Areas/Admin — **Tentative**, screens inferred from admin folder structure and DB log model). For production you would bolt on OTel/serilog externally.

---

## 9. Testing & quality gates

- **Types present** (FACT): a single NUnit project with per-layer subfolders (`Nop.Core.Tests`, `Nop.Data.Tests`, `Nop.Services.Tests`, `Nop.Web.Tests`) and a shared `BaseNopTest` bootstrapping a SQLite data provider (src/Tests/Nop.Tests/). **INFERENCE**: these are unit/service-integration hybrids (real DI + SQLite DB) rather than pure mocks; Moq is available for the rest.
- **How they run in CI** (FACT): GitHub Actions on push/PR to `develop`: restore → build Release → `dotnet test` (.github/workflows/dotnet.yml). No coverage gates, linting, or deployment steps in the workflow.
- **Coverage gaps / risks** (FACT on counts): **153 test files vs ~2,548 production .cs files** in Libraries+Presentation (file-count ratio, not line coverage). No e2e/UI tests found; no tests per plugin project found (Tests folder contains only Nop.Tests). **INFERENCE**: controllers, Razor views, admin area, and plugins are essentially untested in-repo; the safest change surface is Nop.Services where test scaffolding exists.

---

## 10. Migration hotspots / tech debt (evidence-based)

1. **`OrderProcessingService` god class + hidden side-effects**
   - Evidence: 3,561 lines in one file (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs); `PlaceOrderAsync` orchestrates payment, persistence, inventory movement, notifications, events, and recurring payments inline (:1594-1651).
   - Risk type: Mixed concerns | Hidden side-effects. Confidence: **High**.
   - Refactoring seam: it already publishes `OrderPlacedEvent` — extract post-order side-effects (notifications, activity log, discount/gift-card history) into event consumers behind the existing `IEventPublisher`.
   - Facade: keep `IOrderProcessingService.PlaceOrderAsync` as the stable facade; introduce `IOrderPlacementPipeline` steps internally. Test strategy: characterization tests over `PlaceOrderAsync` using the existing SQLite test harness (BaseNopTest) before splitting. Migration risk: **High** (money path).

2. **Sync-over-async + process-local mutex in order placement**
   - Evidence: `.Result` / `.Wait()` inside the mutex block and an OS-named `Mutex` for dedup (OrderProcessingService.cs:1680-1718, with an in-code comment explaining the constraint).
   - Risk type: Hidden side-effects | Coupling (to single-node hosting). Confidence: **High** (2 refs: code + README's web-farm claim at README.md:17 implies multi-node deployments where this doesn't protect).
   - Seam: replace with a distributed lock keyed through `IStaticCacheManager` (already used for the interval flag). Facade: none needed. Tests: concurrency test with two parallel `PlaceOrderAsync` calls. Risk: **Med**.

3. **Service-locator pattern (`EngineContext.Current` / `Singleton<T>`)**
   - Evidence: 137 usages of `EngineContext.Current` across src (grep count); `Singleton<AppSettings>.Instance` read directly in Program.cs:25; EngineContext at src/Libraries/Nop.Core/Infrastructure/EngineContext.cs:19-46.
   - Risk type: Coupling | Shared mutable state. Confidence: **High**.
   - Seam: constructor injection is already the norm in services (see CheckoutController's DI fields) — burn down `EngineContext.Current` call sites module by module. Risk: **Med** (mechanical but wide).

4. **Background jobs via self-HTTP inside the web process**
   - Evidence: TaskScheduler posts to `scheduletask/runtask` on the store's own public URL (TaskScheduler.cs:67,206; NopTaskDefaults.cs:11; ScheduleTaskController.cs:23).
   - Risk type: Configuration risk | Hidden side-effects (depends on store URL being correct/reachable from itself; jobs silently stop when app idles). Confidence: **High**.
   - Seam/extraction candidate: `IScheduleTaskRunner` is already an interface — extract a hosted-service (`IHostedService`) or external worker that calls the runner directly, bypassing HTTP. Facade: keep `IScheduleTaskRunner`. Tests: task-runner unit tests exist scaffolding-wise (Services test folder). Risk: **Low–Med**.

5. **Runtime-writable configuration & plugin directory in the app folder**
   - Evidence: app writes `App_Data/appsettings.json` (AppSettingsHelper.cs:28; DataSettingsManager.cs:111-119); plugins build straight into `Nop.Web/Plugins` (Payments.Manual.csproj:12-13); Dockerfile must chmod app dirs writable (Dockerfile:20-33).
   - Risk type: Configuration risk | Coupling (immutable-infrastructure unfriendly; complicates web farms and read-only file systems). Confidence: **High**.
   - Seam: config already flows through `IConfiguration` + env vars (Program.cs:20) — move secrets to env/vault and treat file-write paths as install-time only. Risk: **Med**.

6. **Reflection-based plugin discovery affecting startup**
   - Evidence: `NopEngine` scans all types for `INopStartup`/`IStartupTask` including plugin assemblies (NopEngine.cs:40-150); custom `ClearPluginAssemblies` build step exists to manage plugin DLL conflicts (src/Build/).
   - Risk type: Coupling | Hidden side-effects. Confidence: **Medium** (behavioral blast radius is inferred, not demonstrated).
   - Seam: plugin surface is already interface-driven (`IPluginService`, plugin base classes in Nop.Services/Plugins/) — candidates for out-of-process extraction are the plugin categories with clean interfaces: **payments, shipping, tax** providers. Risk: **High** (ecosystem compatibility).

7. **Thin test coverage relative to code size**
   - Evidence: 153 test files vs ~2,548 production files (find counts); CI runs tests but no coverage threshold (.github/workflows/dotnet.yml).
   - Risk type: Lack of tests. Confidence: **High** for the ratio; **Low** for any specific untested-module claim without per-module inspection.

**Candidate boundaries for extraction (Strangler-friendly), summarized**
- Scheduled tasks → hosted service/worker behind `IScheduleTaskRunner` (Low–Med risk).
- Post-order side-effects → event consumers behind `IEventPublisher`/`OrderPlacedEvent` (Med).
- Payment/shipping/tax providers → keep plugin interfaces as the facade; strangle individual providers to external services via `Misc.WebApi.Frontend`-style adapters (High).
- Media/image processing (SkiaSharp usage, Nop.Services.csproj:25-27 + Media/ folder) → stateless service candidate (**Tentative** — internal call graph not inspected).

---

## 11. Open questions

- **UNKNOWN**: exact env-var naming convention for overriding `AppSettings` sections → inspect `AppSettingsHelper.cs` and `IConfig` binding.
- **UNKNOWN**: when exactly migrations run (startup vs installer vs admin action) → read `Nop.Data/NopDbStartup.cs` and `Migrations/MigrationManager.cs` end to end.
- **UNKNOWN**: what writes to the `logs/` directory created in Docker (Dockerfile:18) → grep for file-logging sinks and check plugin code.
- **UNKNOWN**: production deployment beyond Docker/Kudu (no k8s manifests in repo) → check nopCommerce docs / your hosting repo.
- **Tentative**: whether gulp must be run for local dev or assets are committed → inspect `wwwroot` contents vs `gulpfile.js` tasks.
- **Tentative**: payment-succeeded-but-save-failed compensation behavior → trace `SaveOrderDetailsAsync` and payment plugin refund/void paths (`IPaymentService`).
- **UNKNOWN**: role/permission granularity in practice → read `StandardPermission.cs` and `PermissionService.cs` (src/Libraries/Nop.Services/Security/).

---

## 12. First 90 minutes checklist

1. (10 min) Read this file, then skim `src/NopCommerce.sln` layout and confirm the 5-project layering in §6.
2. (10 min) Read `src/Presentation/Nop.Web/Program.cs` (50 lines) and `Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs` to see the whole request pipeline.
3. (15 min) `docker-compose up` (or point `App_Data/appsettings.json` at a local DB), hit `/`, and walk the web installer (`InstallController`).
4. (10 min) `dotnet test src` — confirm the SQLite-backed test harness works on your machine; open `src/Tests/Nop.Tests/BaseNopTest.cs`.
5. (15 min) Trace one request: `CatalogController` → a `Factories/` model factory → an `Nop.Services/Catalog` service → `IRepository<T>`. This is the canonical shape of ~everything.
6. (15 min) Read `OrderProcessingService.PlaceOrderAsync` (:1594-1720) — the most business-critical method in the codebase, and a compact tour of its idioms (events, activity log, settings objects, caching, localization).
7. (10 min) Open one small plugin (e.g. `Nop.Plugin.Payments.Manual`) to learn the plugin contract and the OutputPath-into-Nop.Web build trick.
8. (5 min) Bookmark: `App_Data/appsettings.json` (real config), `scheduletask/runtask` (background jobs), `Areas/Admin` (the other half of the app).

---

# Project Orientation (Generated)
## 1. What this project is
- Purpose: `nopCommerce` appears to be an ASP.NET Core eCommerce platform with a public storefront, an admin area, and a plugin model for integrations and feature extensions. (README.md:4-18) (src/Presentation/Nop.Web/Nop.Web.csproj:7-18) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ServiceCollectionExtensions.cs:107-115)
- Who uses it / main use-cases: The committed routes and services show storefront flows such as catalog, cart, checkout, customer account, blog, contact, and admin/security/settings management. (src/Presentation/Nop.Web/Infrastructure/RouteProvider.cs:29-117) (src/Presentation/Nop.Web.Framework/Infrastructure/NopStartup.cs:138-243)

## 2. Quick start (developer)
- Prereqs: the repo pins `.NET SDK 10.0.100`, the CI workflow installs `.NET 10.0.x`, and the Docker build/runtime images are also `.NET 10`; the root README still says `.NET 9`, so treat the repo files as the fresher source. (global.json:1-7) (.github/workflows/dotnet.yml:19-28) (Dockerfile:1-16) (Dockerfile:35-54) (README.md:13-16)
- How to run locally: the committed local startup path with the clearest evidence is Docker Compose, using the main app container plus one database container, with variants for SQL Server, MySQL, and PostgreSQL. (docker-compose.yml:1-18) (mysql-docker-compose.yml:1-17) (postgresql-docker-compose.yml:1-17)
- How to run locally: for non-Docker development, the committed docs say to open the solution, rebuild, and publish `Nop.Web`; a repo-owned `launchSettings.json` or other explicit local run script was not found during this pass. (src/NopCommerce.sln:6-23) (src/Deploying.Readme.txt:1-5)
- How to run tests: CI runs `dotnet restore src`, `dotnet build --configuration Release src`, and `dotnet test --configuration Release src`. (.github/workflows/dotnet.yml:23-28)
- Common pitfalls: if the database is not configured, middleware redirects requests to the installation path, so first boot is an install flow rather than the storefront. (src/Libraries/Nop.Data/DataSettingsManager.cs:141-149) (src/Libraries/Nop.Services/Installation/InstallUrlMiddleware.cs:35-50)
- Common pitfalls: app settings are loaded from `App_Data/appsettings.json`, then `App_Data/appsettings.{Environment}.json`, then environment variables, so local behavior can differ from defaults if machine-level variables are set. (src/Presentation/Nop.Web/Program.cs:14-20) (src/Libraries/Nop.Core/Configuration/NopConfigurationDefaults.cs:8-17)
- Common pitfalls: installation validates write permissions on required directories/files before proceeding, so filesystem permissions can block local setup. (src/Presentation/Nop.Web/Controllers/InstallController.cs:165-190)

## 3. Architecture at a glance
- High-level component diagram:

```text
Browser / Admin UI
        |
        v
   Nop.Web (controllers, routes, views)
        |
        v
Nop.Services + Nop.Web.Framework
        |
        v
 Nop.Data repositories + migrations
        |
        v
 SQL Server / MySQL / PostgreSQL

Plugins extend MVC parts, routes, auth, events, and integrations.
In-process scheduler posts back into the web app to run tasks.
```

- The main runtime process is a single ASP.NET Core web application started from `Program.Main`, with request pipeline setup delegated through the engine/startup infrastructure. (src/Presentation/Nop.Web/Program.cs:10-50) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:52-67)
- Runtime processes: no separate worker service or queue consumer was identified in the solution; background work appears to be driven by an in-process scheduler that posts to `ScheduleTaskController.RunTask`. (src/NopCommerce.sln:12-23) (src/Presentation/Nop.Web.Framework/Infrastructure/AppStartedConsumer.cs:61-90) (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:47-123) (src/Presentation/Nop.Web/Controllers/ScheduleTaskController.cs:21-33)
- Data stores and queues: the core data provider manager supports SQL Server, MySQL, and PostgreSQL; optional distributed cache backends are memory, SQL Server, Redis, and Redis-synchronized memory. No message broker abstraction was identified in core startup. (src/Libraries/Nop.Data/DataProviderManager.cs:20-28) (src/Presentation/Nop.Web.Framework/Infrastructure/NopStartup.cs:97-129) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ServiceCollectionExtensions.cs:197-236)

## 4. Repository map
- Top-level directories: `.github/` holds CI, `src/` holds the solution and application code, and `upgradescripts/` appears reserved for upgrade artifacts. (.github) (src) (upgradescripts)
- `src/Presentation/Nop.Web`: presentation layer for public store and admin area; main public entrypoint is `Program.Main`; main route surface is `RouteProvider`; key files are `Program.cs`, `Infrastructure/RouteProvider.cs`, and controllers such as `CheckoutController.cs` and `InstallController.cs`. (src/Presentation/Nop.Web/Nop.Web.csproj:7-18) (src/Presentation/Nop.Web/Program.cs:10-50) (src/Presentation/Nop.Web/Infrastructure/RouteProvider.cs:18-117)
- `src/Presentation/Nop.Web.Framework`: shared web infrastructure for startup, middleware, MVC extensions, routing helpers, and framework-level consumers; key public seams include `INopStartup` implementations, middleware extensions, and route publishing. (src/NopCommerce.sln:12-23) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ServiceCollectionExtensions.cs:96-138) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:69-117) (src/Presentation/Nop.Web.Framework/Infrastructure/AppStartedConsumer.cs:61-90)
- `src/Libraries/Nop.Core`: core classes and business objects such as caching, events, helpers, and domain entities. (src/Libraries/Nop.Core/Nop.Core.csproj:3-25)
- `src/Libraries/Nop.Data`: data-access layer with repositories, provider selection, and FluentMigrator-based migrations; main public seams are `IDataProviderManager`, `INopDataProvider`, `IRepository<>`, and `IMigrationManager`. (src/Libraries/Nop.Data/Nop.Data.csproj:3-26) (src/Libraries/Nop.Data/NopDbStartup.cs:38-72) (src/Libraries/Nop.Data/DataProviderManager.cs:20-45)
- `src/Libraries/Nop.Services`: business logic layer; `NopStartup` wires a large service catalog here, including catalog, customer, order, payment, tax, shipping, GDPR, localization, messages, plugins, and scheduling. (src/Libraries/Nop.Services/Nop.Services.csproj:3-38) (src/Presentation/Nop.Web.Framework/Infrastructure/NopStartup.cs:137-243)
- `src/Plugins`: plugin projects compiled into `Presentation/Nop.Web/Plugins/...`; committed examples cover payments, tax, external auth, MFA, search, storage, analytics, and misc integrations. (src/NopCommerce.sln:20-84) (src/Plugins/Nop.Plugin.Payments.PayPalCommerce/Nop.Plugin.Payments.PayPalCommerce.csproj:12-18) (src/Plugins/Nop.Plugin.Tax.Avalara/Nop.Plugin.Tax.Avalara.csproj:12-18) (src/Plugins/Nop.Plugin.MultiFactorAuth.GoogleAuthenticator/Nop.Plugin.MultiFactorAuth.GoogleAuthenticator.csproj:12-18) (src/Plugins/Nop.Plugin.Misc.AzureBlob/Nop.Plugin.Misc.AzureBlob.csproj:12-18)
- `src/Tests/Nop.Tests`: a single test project organized into `Nop.Core.Tests`, `Nop.Data.Tests`, `Nop.Services.Tests`, and `Nop.Web.Tests`; base fixtures spin up a test DI container and initialize a database. (src/Tests/Nop.Tests/Nop.Tests.csproj:14-28) (src/Tests/Nop.Tests/BaseNopTest.cs:170-227) (src/Tests/Nop.Tests/BaseNopTest.cs:561-610)
- `src/Build/src/ClearPluginAssemblies`: build-time utility that removes copied assemblies and runtime folders from plugin output directories after build/publish. (src/Build/src/ClearPluginAssemblies/ClearPluginAssemblies.csproj:3-9) (src/Build/src/ClearPluginAssemblies/Program.cs:5-46) (src/Build/src/ClearPluginAssemblies/Program.cs:80-127)

## 5. Critical flows
- Flow: First-run installation.
- Trigger/entrypoint: request hits the app before a database connection string exists, so `InstallUrlMiddleware` redirects to the install path and `InstallController` serves the install flow. (src/Libraries/Nop.Data/DataSettingsManager.cs:141-149) (src/Libraries/Nop.Services/Installation/InstallUrlMiddleware.cs:35-50) (src/Presentation/Nop.Web/Controllers/InstallController.cs:128-150)
- Steps: the POST path validates filesystem permissions, builds/saves `DataConfig`, optionally creates the database, initializes schema, optionally downloads locale assets/newsletter signup, runs `InstallationService.InstallAsync`, and prepares plugins for installation. (src/Presentation/Nop.Web/Controllers/InstallController.cs:172-208) (src/Presentation/Nop.Web/Controllers/InstallController.cs:210-228) (src/Presentation/Nop.Web/Controllers/InstallController.cs:233-318)
- Side effects: writes app settings, initializes database schema, seeds install data, and stages plugin installation. (src/Libraries/Nop.Data/DataSettingsManager.cs:135-149) (src/Presentation/Nop.Web/Controllers/InstallController.cs:202-208) (src/Presentation/Nop.Web/Controllers/InstallController.cs:228-318)
- Failure modes: missing write permission, bad connection string, database creation/existence failure, and install-time external calls that are swallowed or surfaced depending on the step. (src/Presentation/Nop.Web/Controllers/InstallController.cs:172-190) (src/Presentation/Nop.Web/Controllers/InstallController.cs:197-225) (src/Presentation/Nop.Web/Controllers/InstallController.cs:250-285)
- Flow: HTTP request pipeline and route resolution.
- Trigger/entrypoint: `Program.Main` builds the app, configures the request pipeline, publishes `AppStartedEvent`, and starts Kestrel-hosted ASP.NET Core execution. (src/Presentation/Nop.Web/Program.cs:10-50)
- Steps: middleware setup includes exception handling, static files, install redirect, localization, authentication, authorization, and endpoint routing; route registration is delegated through `IRoutePublisher` and `RouteProvider`. (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:73-117) (src/Presentation/Nop.Web.Framework/Infrastructure/AuthenticationStartup.cs:19-36) (src/Presentation/Nop.Web.Framework/Infrastructure/AuthorizationStartup.cs:22-30) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:466-473) (src/Presentation/Nop.Web/Infrastructure/RouteProvider.cs:18-117)
- Side effects: error logging, static file serving from `Themes`, `Plugins`, images, and backups, plus proxy/header handling when enabled. (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:89-117) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:264-365) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:476-531)
- Failure modes: unhandled exceptions are logged then rethrown, 404/400 responses can be logged, and misconfigured proxy settings could affect forwarded headers. (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:73-117) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:123-200) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:206-217) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:480-531)
- Flow: Checkout to order placement.
- Trigger/entrypoint: `CheckoutController.ConfirmOrder` loads the current cart/customer/store, enforces guest/captcha/minimum-interval rules, restores the `ProcessPaymentRequest`, and calls `IOrderProcessingService.PlaceOrderAsync`. (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:1276-1333)
- Steps: `OrderProcessingService` prepares and validates customer/cart/billing/shipping/totals, processes payment, saves order details, moves cart items into order items, adjusts inventory, records discount/gift-card usage, clears checkout data, publishes `OrderPlacedEvent`, and post-processes payment. (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:282-309) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:339-395) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1303-1339) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1388-1423) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1567-1692) (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:1332-1349)
- Side effects: creates orders/order items, reduces inventory, clears the shopping cart, writes activity/log records, raises events, and can redirect into payment-provider post-processing. (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1327-1339) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1606-1624) (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:1337-1349)
- Failure modes: invalid cart/totals, missing payment method, payment processor errors, duplicate-order protection via mutex/cache, and inventory-sensitive failures make this a high-cost path. (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:363-395) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1393-1418) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1634-1648) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1652-1692)
- Flow: Scheduled task execution.
- Trigger/entrypoint: on `AppStartedEvent`, `AppStartedConsumer` initializes and starts the task scheduler; each scheduled run becomes an HTTP POST back into `ScheduleTaskController.RunTask`. (src/Presentation/Nop.Web.Framework/Infrastructure/AppStartedConsumer.cs:61-90) (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:47-123) (src/Presentation/Nop.Web/Controllers/ScheduleTaskController.cs:21-33)
- Steps: the scheduler loads enabled tasks, computes next-run timing, builds a URL from the current store, and periodically posts `taskType`; the controller resolves the task and delegates to `IScheduleTaskRunner`. (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:55-110) (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:188-223) (src/Presentation/Nop.Web/Controllers/ScheduleTaskController.cs:23-31)
- Side effects: any installed task can execute arbitrary business logic; examples in the repo include queued email sending, Avalara tax-rate download, and Zettle sync. (src/Libraries/Nop.Services/Messages/QueuedMessagesSendTask.cs:1-25) (src/Plugins/Nop.Plugin.Tax.Avalara/Services/DownloadTaxRatesTask.cs:1-8) (src/Plugins/Nop.Plugin.Misc.Zettle/Services/ZettleSyncTask.cs:1-8)
- Failure modes: if store URL, task timing, or task execution fails, the scheduler logs a formatted error but continues running. (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:67-72) (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:197-224)

## 6. Dependencies
- Internal module dependencies: `Nop.Web` depends on `Nop.Core`, `Nop.Data`, `Nop.Services`, and `Nop.Web.Framework`; `Nop.Services` depends on `Nop.Core` and `Nop.Data`; `Nop.Data` depends on `Nop.Core`. (src/Presentation/Nop.Web/Nop.Web.csproj:21-26) (src/Libraries/Nop.Services/Nop.Services.csproj:31-34) (src/Libraries/Nop.Data/Nop.Data.csproj:24-26)
- External services: first-class database providers are SQL Server, MySQL, and PostgreSQL. Optional distributed cache backends include SQL Server and Redis. (src/Libraries/Nop.Data/DataProviderManager.cs:20-28) (src/Presentation/Nop.Web.Framework/Infrastructure/NopStartup.cs:97-129) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ServiceCollectionExtensions.cs:205-235)
- External services: the plugin set shows committed integration points for PayPal Commerce, Avalara, Azure Blob storage, Facebook auth, Google Analytics, and Google Authenticator MFA. (src/NopCommerce.sln:24-84) (src/Plugins/Nop.Plugin.Payments.PayPalCommerce/Nop.Plugin.Payments.PayPalCommerce.csproj:12-18) (src/Plugins/Nop.Plugin.Tax.Avalara/Nop.Plugin.Tax.Avalara.csproj:12-18) (src/Plugins/Nop.Plugin.Misc.AzureBlob/Nop.Plugin.Misc.AzureBlob.csproj:12-18) (src/Plugins/Nop.Plugin.MultiFactorAuth.GoogleAuthenticator/Nop.Plugin.MultiFactorAuth.GoogleAuthenticator.csproj:12-18)
- Build/deploy dependencies: build tooling includes FluentMigrator, linq2db, Autofac integration, Docker, GitHub Actions, and a Node/Kudu-based deployment script for one deployment path. (src/Libraries/Nop.Data/Nop.Data.csproj:14-21) (src/Libraries/Nop.Core/Nop.Core.csproj:14-24) (Dockerfile:1-54) (.github/workflows/dotnet.yml:12-28) (src/deploy.cmd:8-79)

## 7. Configuration & environments
- Where config lives: startup reads `App_Data/appsettings.json`, then `App_Data/appsettings.{Environment}.json`, then environment variables. (src/Presentation/Nop.Web/Program.cs:14-20) (src/Libraries/Nop.Core/Configuration/NopConfigurationDefaults.cs:8-17)
- Where config lives: data settings are loaded from app settings and can be saved back into app settings during install or admin configuration. (src/Libraries/Nop.Data/DataSettingsManager.cs:100-138) (src/Presentation/Nop.Web/Controllers/InstallController.cs:202-208)
- Environment variables: the admin settings model explicitly checks for environment variables using the `ConnectionStrings__...` naming convention and similar section/property mappings. (src/Presentation/Nop.Web/Areas/Admin/Factories/SettingModelFactory.cs:922-929)
- Secrets handling: connection strings, encryption keys, and plugin/API credentials appear to be configured through app settings and settings models rather than a dedicated secret-store abstraction in core code. (src/Libraries/Nop.Data/Configuration/DataConfig.cs:9-20) (src/Libraries/Nop.Services/Security/EncryptionService.cs:121-153) (src/Presentation/Nop.Web/Areas/Admin/Factories/SettingModelFactory.cs:246-248) (src/Presentation/Nop.Web/Areas/Admin/Factories/SettingModelFactory.cs:617-617)
- Secrets handling: ASP.NET Core data protection keys are persisted under `App_Data/DataProtectionKeys`. (src/Libraries/Nop.Core/Security/NopDataProtectionDefaults.cs:8-11) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ServiceCollectionExtensions.cs:242-249)
- Local vs prod differences: Docker runs the published app on port 80, while proxy/header behavior is conditional on `HostingConfig.UseProxy`; Kudu deployment publishes `Nop.Web` into a target directory. (Dockerfile:47-54) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:480-531) (src/deploy.cmd:67-79)

## 8. Observability
- Logging: the default logger writes `Log` records through `IRepository<Log>` and can include IP address, page URL, referrer, and customer ID. (src/Libraries/Nop.Services/Logging/DefaultLogger.cs:17-21) (src/Libraries/Nop.Services/Logging/DefaultLogger.cs:68-80) (src/Libraries/Nop.Services/Logging/DefaultLogger.cs:211-235)
- Logging: unhandled exceptions, 404s, and 400s are logged from middleware. (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:89-117) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:140-151) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:208-217)
- Metrics: Unknown. Inspect hosting infrastructure, plugins, and any external monitoring repo next; no metrics-specific wiring was identified in the main startup/pipeline files reviewed here. (src/Presentation/Nop.Web/Program.cs:10-50) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ServiceCollectionExtensions.cs:96-138) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:52-545)
- Tracing: Unknown. Inspect infrastructure/runtime configuration next; no tracing-specific wiring was identified in the main startup/pipeline files reviewed here. (src/Presentation/Nop.Web/Program.cs:10-50) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ServiceCollectionExtensions.cs:96-138)
- Alerts: Unknown. Inspect deployment/infrastructure automation next; the application repo shows CI, Docker, and Kudu deployment paths, but no alert definitions. (.github/workflows/dotnet.yml:1-28) (Dockerfile:1-54) (src/deploy.cmd:1-106)

## 9. Testing & quality gates
- Types of tests present: the test project is organized into core, data, services, and web test suites, using NUnit with Moq and AwesomeAssertions. (src/Tests/Nop.Tests/Nop.Tests.csproj:14-24) (src/Tests/Nop.Tests/Nop.Tests.csproj:26-28) (src/Tests/Nop.Tests/Nop.Core.Tests) (src/Tests/Nop.Tests/Nop.Data.Tests) (src/Tests/Nop.Tests/Nop.Services.Tests) (src/Tests/Nop.Tests/Nop.Web.Tests)
- How tests run in CI: the GitHub workflow restores, builds, and runs `dotnet test` for `src` on pushes and PRs to `develop`. (.github/workflows/dotnet.yml:6-28)
- Test runtime strategy: `BaseNopTest` builds a DI container, creates and initializes a database, runs installation with sample data, and defaults `DataProviderType.Unknown` to an in-memory shared SQLite connection string. (src/Tests/Nop.Tests/BaseNopTest.cs:108-135) (src/Tests/Nop.Tests/BaseNopTest.cs:161-227) (src/Tests/Nop.Tests/BaseNopTest.cs:579-610) (src/Tests/Nop.Tests/NopTestConfiguration.cs:23-27)
- Coverage gaps / risks: plugin-specific test projects were not evident under `src/Tests/Nop.Tests`; the committed test tree centers on Core/Data/Services/Web rather than `src/Plugins`. (src/Tests/Nop.Tests) (src/Plugins)
- Coverage gaps / risks: the CI workflow enforces build-and-test only; no committed coverage threshold, lint gate, or security scan was identified in `.github/workflows/dotnet.yml`. (.github/workflows/dotnet.yml:12-28)

## 10. Migration hotspots / tech debt (evidence-based)
- Hotspot: `OrderProcessingService` is a 3,532-line service with a very wide constructor and responsibilities spanning validation, totals, payment, persistence, inventory, notifications, events, and status changes; this is a high-value extraction target because order bugs are financially costly. Suggested facade interface: `IOrderPlacementOrchestrator.PlaceAsync(ProcessPaymentRequest)`. Test strategy: approval-style flow tests around successful order placement plus failure injection for payment/totals/inventory. Migration risk: High. (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:40-92) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:97-194) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:282-309) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1567-1692)
- Hotspot: `CheckoutController` is a 2,186-line controller that owns step orchestration, address edits, shipping/payment selection, captcha, and order submission; a strangler seam is a `ICheckoutFlowService` or step handlers per checkout stage. Test strategy: controller tests around step transitions and service tests for each extracted step. Migration risk: High. (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:33-65) (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:268-338) (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:342-405) (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:1276-1363)
- Hotspot: `RouteProvider` is a 703-line monolithic route registry; candidate boundary is route groups by domain such as account, catalog, checkout, and content. Suggested facade interface: `IRouteGroup.Register(IEndpointRouteBuilder)`. Test strategy: route registration smoke tests per group. Migration risk: Medium. (src/Presentation/Nop.Web/Infrastructure/RouteProvider.cs:18-117)
- Hotspot: `NopStartup` manually registers a very large service graph in one file; candidate boundary is feature-specific DI modules or `INopStartup` slices per subsystem. Suggested facade interface: feature-level startup classes that expose `ConfigureServices(IServiceCollection, IConfiguration)`. Test strategy: container smoke tests verifying expected service registrations. Migration risk: Medium. (src/Presentation/Nop.Web.Framework/Infrastructure/NopStartup.cs:73-319)
- Hotspot: documentation/runtime drift exists around target framework version, which increases onboarding friction and deployment risk. Suggested seam: make `global.json` and CI/runtime version sources authoritative and generate docs from them where possible. Test strategy: CI check that README version references match `global.json` and workflow/Docker versions. Migration risk: Low. (README.md:13-16) (global.json:1-7) (.github/workflows/dotnet.yml:19-28) (Dockerfile:1-16)

## 11. Open questions
- Unknown: exact non-Docker local run command expected by the team. Inspect contributor docs or developer runbooks next; committed docs only cover rebuild/publish and Docker/Kudu paths. (src/Deploying.Readme.txt:1-5) (docker-compose.yml:1-18) (src/deploy.cmd:67-79)
- Unknown: production topology beyond a single web container or Kudu publish target. Inspect infra repo, hosting manifests, or deployment pipeline definitions next. (Dockerfile:35-54) (src/deploy.cmd:23-79)
- Unknown: whether metrics, tracing, and alerts are configured outside this repo. Inspect hosting platform and observability tooling next. (src/Presentation/Nop.Web/Program.cs:10-50) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:52-545)
- Unknown: whether plugin integrations have dedicated contract/integration tests elsewhere. Inspect plugin repos or broader CI configuration next. (src/Plugins) (src/Tests/Nop.Tests)
- Unknown: exact secrets-management standard in production. Inspect infrastructure configuration, secret-store bindings, and deployment docs next. (src/Presentation/Nop.Web/Program.cs:14-20) (src/Presentation/Nop.Web/Areas/Admin/Factories/SettingModelFactory.cs:922-929)

## 12. First 90 minutes checklist
- Open `src/NopCommerce.sln`, then read `src/Presentation/Nop.Web/Program.cs`, `src/Presentation/Nop.Web.Framework/Infrastructure/NopStartup.cs`, and `src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs` to anchor startup and middleware flow. (src/NopCommerce.sln:6-23) (src/Presentation/Nop.Web/Program.cs:10-50) (src/Presentation/Nop.Web.Framework/Infrastructure/NopStartup.cs:73-319) (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ApplicationBuilderExtensions.cs:52-545)
- Decide whether you will boot via Docker or a local publish/build path, and confirm which database provider you are using first. (docker-compose.yml:1-18) (mysql-docker-compose.yml:1-17) (postgresql-docker-compose.yml:1-17) (src/Libraries/Nop.Data/DataProviderManager.cs:20-28)
- Walk the install flow before changing features so you understand how `DataConfig` and app settings are written. (src/Presentation/Nop.Web/Controllers/InstallController.cs:152-318) (src/Libraries/Nop.Data/DataSettingsManager.cs:100-149)
- Skim `RouteProvider.cs` and the relevant controller for your area before editing UI flows, because routing is centralized and broad. (src/Presentation/Nop.Web/Infrastructure/RouteProvider.cs:18-117)
- If you touch checkout or orders, read `CheckoutController.cs` and `OrderProcessingService.cs` together first; they are tightly coupled along the happy path and failure path. (src/Presentation/Nop.Web/Controllers/CheckoutController.cs:1276-1363) (src/Libraries/Nop.Services/Orders/OrderProcessingService.cs:1567-1692)
- If you touch background behavior, read `AppStartedConsumer`, `TaskScheduler`, and `ScheduleTaskController` before assuming a worker architecture exists. (src/Presentation/Nop.Web.Framework/Infrastructure/AppStartedConsumer.cs:61-90) (src/Libraries/Nop.Services/ScheduleTasks/TaskScheduler.cs:47-123) (src/Presentation/Nop.Web/Controllers/ScheduleTaskController.cs:21-33)
- Run the existing test suite entrypoint from CI commands, then narrow to the relevant `Nop.*.Tests` area before making changes. (.github/workflows/dotnet.yml:23-28) (src/Tests/Nop.Tests)
- Check active plugins and settings relevant to your feature early, because plugin loading and event consumers materially change runtime behavior. (src/Presentation/Nop.Web.Framework/Infrastructure/Extensions/ServiceCollectionExtensions.cs:107-115) (src/Presentation/Nop.Web.Framework/Infrastructure/NopStartup.cs:253-310)